import React from "react";

export default class About extends React.Component {
  render() {
    return (
      <p>LeagueSpot is awesome, you should use it. We really need to figure out our data source though, cause 1000 hits a month is not enough.</p>
    );
  }
}
